/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',          // write fully-static site into  out/  on next build
  // images: { unoptimized: true },   // uncomment if you use <Image>
  // trailingSlash: true,             // uncomment if you want /about/ not /about
};
module.exports = nextConfig;
